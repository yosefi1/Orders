# פתרון - מייל לא מגיע

## 🔍 בדיקות מהירות:

### 1. בדוק את SUPPLIER_EMAIL ב-Vercel
1. לך ל-Vercel Dashboard → Settings → Environment Variables
2. מצא את `SUPPLIER_EMAIL`
3. ודא שהערך הוא: `yosef.tal@altera.com,Giladx.schossberger@intel.com`
4. **חשוב:** אין רווחים מיותרים, רק פסיק בין המיילים

### 2. בדוק את הלוגים ב-Vercel
1. לך ל-Vercel Dashboard → Functions → `/api/cron/daily-report`
2. לחץ על הקריאה האחרונה
3. חפש את השורה: `Daily report email sent successfully to...`
4. תראה בדיוק לאילו מיילים נשלח

### 3. בדוק את תיבת ה-Spam
- המייל יכול להיות בתיבת ה-Spam
- חפש: "Daily Orders Report" או "קפיטריית אינטל"

### 4. בדוק אם המייל נשלח בכלל
- אם גלעד קיבל → הבעיה ספציפית למייל שלך
- אם אף אחד לא קיבל → הבעיה ב-SMTP או בהגדרות

## 🛠️ פתרונות:

### פתרון 1: ודא שהערך נכון ב-Vercel
הערך צריך להיות בדיוק כך (ללא רווחים מיותרים):
```
yosef.tal@altera.com,Giladx.schossberger@intel.com
```

**לא:**
- `yosef.tal@altera.com, Giladx.schossberger@intel.com` (רווח אחרי פסיק)
- `yosef.tal@altera.com , Giladx.schossberger@intel.com` (רווח לפני פסיק)

### פתרון 2: בדוק את הלוגים
בלוגים תראה בדיוק לאילו מיילים נשלח. אם אתה לא רואה את המייל שלך שם, הבעיה היא ב-SUPPLIER_EMAIL.

### פתרון 3: נסה לשלוח מייל ידנית
הרץ את הקוד הזה כדי לבדוק:
```javascript
fetch('https://orders-altera.vercel.app/api/cron/daily-report', {
  headers: {
    'Authorization': 'Bearer altera1'
  }
})
.then(r => r.json())
.then(data => console.log(data));
```

ואז לך ללוגים ב-Vercel ותראה לאילו מיילים נשלח.

### פתרון 4: בעיית SMTP
אם המייל נשלח אבל לא מגיע, יכול להיות:
- **Gmail מגביל** - אם יש יותר מדי מיילים, Gmail יכול לחסום
- **Spam filter** - המייל נחסם על ידי ספק המייל
- **בעיית SMTP** - שרת ה-SMTP לא שולח נכון

## 📝 מה לבדוק עכשיו:

1. ✅ מה הערך של SUPPLIER_EMAIL ב-Vercel?
2. ✅ מה הלוגים אומרים (לאילו מיילים נשלח)?
3. ✅ האם גלעד קיבל את המייל?
4. ✅ האם המייל בתיבת ה-Spam?

## 🚀 בדיקה מהירה:

הרץ את הקוד הזה ובדוק את הלוגים:
```javascript
fetch('https://orders-altera.vercel.app/api/cron/daily-report', {
  headers: {
    'Authorization': 'Bearer altera1'
  }
})
.then(r => r.json())
.then(data => {
  console.log('תוצאה:', data);
  // עכשיו לך ל-Vercel Dashboard → Functions → daily-report
  // ותראה לאילו מיילים נשלח
});
```

